# Public site URL (used for canonical / sitemap / Open Graph)
NEXT_PUBLIC_SITE_URL=https://alamalmortgage.ae

# No form-provider env vars are required. Lead forms submit directly from
# the browser to Web3Forms (primary) with FormSubmit as an automatic
# fallback — see src/lib/formSubmit.ts.
